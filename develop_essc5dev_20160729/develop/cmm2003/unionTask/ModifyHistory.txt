2002/8/19, Wolfgang Wang
1。将MngTask1.2升级为1.2.1，因为原有的命令：-kp杀死进程后，显示有误。
2。将UnionTask1.2.c升级为UnionTask1.3.c，主要是增加了一个结构定义：UnionTask，将UnionOutputTasks的输出，输出到一个UnionTask数组。

//	2002/11/17，在1.2基础上升级为1.3，
//	Wolfgang Wang, 将UnionOutputTasks的输出，输出到一个UnionTask数组。
//			增加了以下几个函数：
//				int UnionStartTaskRegistryMaintainer(int Seconds);
//				int UnionTerminateTaskRegistryMaintainer(int Seconds);


// 2002/12/19，王纯军，在1.3基础上升级为2.0版本，主要是将UnionRegisterTask由带固定参数（1个），
// 	改为可带可变个参数

// 2003/02/28，刘杰，在2.0版本基础上升级为2.1版本，主要是在UnionCreateProcess
中增加了一个函数调用setsid，该函数的功能是将进程置为不属于任何一个终端。
